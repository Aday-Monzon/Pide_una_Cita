{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} - Registro {% endblock %}

{% block body %}
    <div class="container h-100">
        <div class="row h-100 justify-content-center align-items-center">
                <div class="col-6 rounded my-3 p-5" style="background-color: #e3f2fd;">
                    <h1 class="h3 mb-3">Registrar Usuario</h1>
                    {{ form(formulario) }}
                    <div class="card-footer">
                        <div class="justify-content-center links">
                            ¿Ya tienes cuenta?<a href="{{ path('app_login') }}">Iniciar Sesi&oacuten</a>
                        </div>
                    </div>
                </div>
        </div>
    </div>
{% endblock %}
